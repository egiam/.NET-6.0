@model Bingo
@{
    ViewData["Title"] = "Bingo .NET";
    var numero = 1;
}

<div class="text-center">
    <h1 class="display-4">Bingo en .NET 6.0</h1>
    @*<p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>*@
    <br />
    <br />
    <br />
    <br />
    @*Generar 4 cartones de bingo de 3*9*@
    <table>
        <tr>
            <td>
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th scope="col">B</th>
                            <th scope="col">I</th>
                            <th scope="col">N</th>
                            <th scope="col">G</th>
                            <th scope="col">O</th>
                        </tr>
                    </thead>
                    <tbody>
                        @for (int i = 0; i < 3; i++)
                        {
                            <tr>
                                @for (int j = 0; j < 9; j++)
                                {
                                    <td class="@numero">@Model.Cartones[0].CartonHecho[i, j]</td>
                                    @(numero++)
                                }
                            </tr>
                        }
                        @(numero = 2)
                    </tbody>
                </table>
            </td>
            <td>
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th scope="col">B</th>
                            <th scope="col">I</th>
                            <th scope="col">N</th>
                            <th scope="col">G</th>
                            <th scope="col">O</th>
                        </tr>
                    </thead>
                    <tbody>
                        @for (int i = 0; i < 3; i++)
                        {
                            <tr>
                                @for (int j = 0; j < 9; j++)
                                {
                                    <td class="@numero">@Model.Cartones[1].CartonHecho[i, j]</td>
                                    @(numero++)
                                }
                            </tr>
                        }
                        @(numero = 1)
                    </tbody>
                </table>
            </td>
            <td>
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th scope="col">B</th>
                            <th scope="col">I</th>
                            <th scope="col">N</th>
                            <th scope="col">G</th>
                            <th scope="col">O</th>
                        </tr>
                    </thead>
                    <tbody>
                        @for (int i = 0; i < 3; i++)
                        {
                            <tr>
                                @for (int j = 0; j < 9; j++)
                                {
                                    <td class="@numero">@Model.Cartones[2].CartonHecho[i, j]</td>
                                    @(numero++)
                                }
                            </tr>
                        }
                        @(numero = 2)
                    </tbody>
                </table>
            </td>
            <td>
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th scope="col">B</th>
                            <th scope="col">I</th>
                            <th scope="col">N</th>
                            <th scope="col">G</th>
                            <th scope="col">O</th>
                        </tr>
                    </thead>
                    <tbody>
                        @for (int i = 0; i < 3; i++)
                        {
                            <tr>
                                @for (int j = 0; j < 9; j++)
                                {
                                    <td class="@numero">@Model.Cartones[3].CartonHecho[i, j]</td>
                                    @(numero++)
                                }
                            </tr>
                        }
                        @(numero = 1)
                    </tbody>
                </table>
            </td>
        </tr>
    </table>
    
</div>
